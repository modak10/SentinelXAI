# --- Milestone 1: Project Setup + Dataset Engineering + EDA ---
# ML/API/UI dependencies (lightgbm, shap, fastapi, streamlit, ...) are
# intentionally deferred to the milestones that need them (see AGENT.md).
pandas==2.2.3
numpy==2.1.3
pyyaml==6.0.2
python-dotenv==1.0.1
matplotlib==3.9.2
pyarrow==18.1.0

# --- Milestone 3: Baseline Models (Logistic Regression, Random Forest, XGBoost) ---
# LightGBM itself is deferred to Milestone 4.
scikit-learn==1.5.2
xgboost==2.1.3
joblib==1.4.2
